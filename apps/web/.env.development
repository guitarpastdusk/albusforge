# Loaded by `next dev` only — never by `next build` or `next start`, and never
# copied into the image. Lets `pnpm dev` run with no gateway and no setup.
# Override in .env.local (e.g. API_MODE=live against a local gateway).
API_MODE=mock
