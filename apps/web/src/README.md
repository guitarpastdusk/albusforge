# src/

Portal source. `app/` routes, `actions/` Server Functions for interactive API calls, `components/` markup, `lib/` logic, `mocks/` mock API data, `styles/` design tokens. `instrumentation.ts` validates runtime config when the server starts. `proxy.ts` is the optimistic session guard for the signed-in routes. `@/` resolves here.
