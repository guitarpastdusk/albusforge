# src/components/landing/

`ChatStart` — the landing page's chat input card and three starter chips. Submitting is a TODO today (it navigates to `/build/mock-build`); in M2 it will `POST /v1/builds` and open the returned build.
