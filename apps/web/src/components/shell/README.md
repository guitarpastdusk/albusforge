# src/components/shell/

- `Header.tsx` — logo, nav pills (active from the pathname; `/live/*` lights Live systems, `/build/*` lights Build), and either Sign in + Get started or the avatar and email. A client component because it reads the pathname.
- `Footer.tsx` — tagline and the Docs / Pricing / Security pledge links.
