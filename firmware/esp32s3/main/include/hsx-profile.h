#pragma once
/* Replaced from the explicit compiler candidate after accepted-plan validation
 * (apps/codegen/src/compiler.ts writes this file from the resolved candidate).
 * These committed values are the ESP32-S3-DevKitC-1 candidate, which is also
 * what a local `idf.py build` of this template produces. */
#define HSX_PROFILE_ID "esp32s3-bh1750-usb-v1"
#define HSX_RUNTIME "0.1.0"
#define HSX_CHANNEL "illuminance"
#define HSX_SDA 8
#define HSX_SCL 9
#define HSX_SENSOR_BH1750 1
#define HSX_SENSOR_BME280 0
#define HSX_SENSOR_SEESAW_SOIL 0
