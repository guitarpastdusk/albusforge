# Copy to secrets.yaml and replace both values with unique random passwords.
# Never commit secrets.yaml, flash backups, or production device credentials.
setup_password: "REPLACE_WITH_UNIQUE_SETUP_PASSWORD"
ota_password: "REPLACE_WITH_UNIQUE_OTA_PASSWORD"
